Simply run the following command to get the version of operator you need.

```bash
curl -sSL "https://raw.githubusercontent.com/pingcap/tidb-operator/v1.5.1/manifests/crd.yaml" > tidb-operator-installation.yaml
```
